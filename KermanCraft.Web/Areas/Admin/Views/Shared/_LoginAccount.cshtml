@using Microsoft.AspNetCore.Identity
@using System.Security.Claims
@using KermanCraft.Domain.Models.People
@inject SignInManager<People> SignInManager
@inject UserManager<People> UserManager


@if (SignInManager.IsSignedIn(User))
{

    {
        var adminUser = await UserManager.FindByIdAsync(User.FindFirstValue(ClaimTypes.NameIdentifier));
<div class="row">

    <div class="col-sm-4">
        <img src="/images/AdminUser/@adminUser.ImageUrl" class="e" alt="لوگو ">
    </div>

    <div class="col-sm-8 dx">
        <a href="#" class="dropdown-toggle dropdown-hover" data-toggle="dropdown">
            @*<img src="/images/AdminUser/@adminUser.ImageUrl" alt="عکس پرفایل" class="img-circle img-responsive " style="width: 10%; height: 10%; " >*@
            <span>سلام! @adminUser.Name عزیز </span>
            <i class="icon-arrow-down"></i>
        </a>
        <ul class="dropdown-menu">
            <li>
                <a asp-action="UpdateAdminUser" asp-controller="Admin" asp-area="Admin" asp-route-id="@User.FindFirstValue(ClaimTypes.NameIdentifier)">
                    <i class="icon-note"></i>
                    ویرایش پروفایل
                </a>
            </li>
            <li>
                <a href="change_password.html">
                    <i class="icon-key"></i>
                    تفییر رمز عبور
                </a>
            </li>
            <li class="divider"></li>
            <li>
                <a asp-action="Logout" asp-controller="Admin" asp-area="Admin">
                    <i class="icon-power"></i>
                    خروج
                </a>
            </li>
        </ul>
        </div>
    </div>
    }
    }
